<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.magiskube.magisk.ui.metro.MetroViewModel" />
    </data>

    <com.google.android.material.card.MaterialCardView
        style="@style/WidgetFoundation.Card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@color/metro_tile_magisk_background">

            <TextView
                android:id="@+id/menu_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="20dp"
                android:text="@string/magisk"
                android:textColor="@color/metro_tile_magisk_on_background"
                android:textSize="24sp"
                android:textStyle="bold" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingBottom="16dp">

                <TextView
                    android:id="@+id/menu_status"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="8dp"
                    android:text="@{viewModel.magiskStatus}"
                    android:textColor="@color/metro_tile_magisk_on_background"
                    android:textSize="16sp"
                    tools:text="Magisk Installed" />

                <TextView
                    android:id="@+id/menu_version"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="16dp"
                    android:text="@{viewModel.magiskVersion}"
                    android:textColor="@color/metro_tile_magisk_on_background"
                    android:textSize="14sp"
                    android:alpha="0.8"
                    tools:text="v25.2 (25200)" />

                <com.google.android.material.button.MaterialButton
                    style="@style/WidgetFoundation.Button.Outlined"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="8dp"
                    android:onClick="@{() -> viewModel.onMagiskInstall()}"
                    android:text="@string/install"
                    android:textAllCaps="false"
                    app:strokeColor="@color/metro_tile_magisk_primary" />

                <com.google.android.material.button.MaterialButton
                    style="@style/WidgetFoundation.Button.Outlined"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="8dp"
                    android:onClick="@{() -> viewModel.onMagiskSettings()}"
                    android:text="@string/settings"
                    android:textAllCaps="false"
                    app:strokeColor="@color/metro_tile_magisk_primary" />

                <com.google.android.material.button.MaterialButton
                    style="@style/WidgetFoundation.Button.Outlined"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:onClick="@{() -> viewModel.onCloseMenu()}"
                    android:text="@string/hide"
                    android:textAllCaps="false"
                    app:strokeColor="@color/metro_tile_magisk_primary" />

            </LinearLayout>

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

</layout>
